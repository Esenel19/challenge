<table class="table table-bordered">
    <tr>
        <td>Prénom</td>
        <td>{{ contact.nom }}</td>
    </tr>
    <tr>
        <td>Nom</td>
        <td>{{ contact.prenom }}</td>
    </tr>
    <tr>
        <td>Email</td>
        <td>{{ contact.email }}</td>
    </tr>
    <tr>
        <td>Message</td>
        <td>{{ contact.contenu }}</td>
    </tr>
</table>

{% for contenu in app.flashes('success') %}
    <div class="col-md-5 mx-auto alert alert-success text-center">
        {{ contenu }}
    </div>
{% endfor %}